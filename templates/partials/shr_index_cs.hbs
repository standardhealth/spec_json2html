<div class="section-heading">
  <h1> 
      The Standard Health Record Code Systems
  </h1> 
</div>
<div class="section-content">
  <div class="container">
    {{#each csysByNamespace as |codesystems|}}
      {{#if codesystems}}
        <div class="row">
        <h2>{{> shr_namespace_reference label=@key suffix="/cs"}}</h2>
        {{> shr_index_cs_element csys=this entryOnly=false}}
        </div>
      {{/if}}
    {{/each}}
  </div>
</div>