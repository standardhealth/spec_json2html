<h2>Parsing Specification shr_v4 for: {{shr_v4.label}}</h2>

<div class="container">
    {{log shr_v4}}
    {{#each shr_v4.children}}
      {{#eq this.label "Namespaces"}}
        {{#each this.children as |child|}}
          {{#if child}}
            {{> schema_namespace_list }}
          {{/if}}
        {{/each}}
      {{/eq}}
    {{/each}}
</div>
